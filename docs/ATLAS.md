# Autonomous Software Factory

ATLAS decomposes product intent into requirements, architecture work, dependency-aware engineering tasks, evidence-based verification, and isolated workspace boundaries. Future execution adapters will use Git worktrees and containers.

## v0.1 boundary

This module currently prioritizes deterministic domain behavior and safe plans. External execution, remote trust, credentials, and destructive operations remain adapter concerns or roadmap items.
