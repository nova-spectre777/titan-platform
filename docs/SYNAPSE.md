# Distributed Compute Fabric

SYNAPSE schedules trusted nodes using resource requirements and leases, adds priority queues, artifact manifests, heartbeat concepts, and explicit lease tokens.

## v0.1 boundary

This module currently prioritizes deterministic domain behavior and safe plans. External execution, remote trust, credentials, and destructive operations remain adapter concerns or roadmap items.
