# Universal Backend Abstraction

OMNIFABRIC selects backend capabilities across providers and includes logical schemas plus migration diff planning so resource abstraction does not erase data structure.

## v0.1 boundary

This module currently prioritizes deterministic domain behavior and safe plans. External execution, remote trust, credentials, and destructive operations remain adapter concerns or roadmap items.
