# Universal Cloud Operating Layer

ORBIT normalizes workloads and provider capacity, scores placements, models network topology, records health probes, and creates failover choices without hiding the reasons behind a decision.

## v0.1 boundary

This module currently prioritizes deterministic domain behavior and safe plans. External execution, remote trust, credentials, and destructive operations remain adapter concerns or roadmap items.
