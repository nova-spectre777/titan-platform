# AI-Native Workspace Runtime

NOVA OS models user intent, workspaces, permissions, sessions, and scoped memory. It is designed to become the operator-facing runtime that invokes other TITAN systems.

## v0.1 boundary

This module currently prioritizes deterministic domain behavior and safe plans. External execution, remote trust, credentials, and destructive operations remain adapter concerns or roadmap items.
